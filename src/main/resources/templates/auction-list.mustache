<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        h1 {
            color: white;
            text-align: center;
            margin-bottom: 40px;
            font-size: 3em;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }

        .items-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 30px;
            margin-bottom: 40px;
        }

        .item-card {
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            cursor: pointer;
        }

        .item-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(0,0,0,0.3);
        }

        .item-name {
            font-size: 1.5em;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
        }

        .item-description {
            font-size: 1.5em;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
            margin-bottom: 15px;
        }

        .item-id {
            color: #666;
            font-size: 0.9em;
            margin-bottom: 10px;
        }

        .item-price {
            font-size: 1.8em;
            color: #667eea;
            font-weight: bold;
            margin: 15px 0;
        }

        .item-status {
            display: inline-block;
            padding: 5px 15px;
            background: #4CAF50;
            color: white;
            border-radius: 20px;
            font-size: 0.9em;
            text-transform: uppercase;
        }

        .btn-view {
            display: block;
            width: 100%;
            padding: 12px;
            margin-top: 15px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1em;
            font-weight: bold;
            cursor: pointer;
            transition: opacity 0.3s ease;
            text-decoration: none;
            text-align: center;
        }

        .btn-view:hover {
            opacity: 0.9;
        }

        .error {
            background: #f44336;
            color: white;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }

        .empty-state h2 {
            color: #666;
            font-size: 2em;
            margin-bottom: 20px;
        }

        .empty-state p {
            color: #999;
            font-size: 1.2em;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>{{title}}</h1>

    {{#error}}
        <div class="error">
            <h2>{{error}}</h2>
        </div>
    {{/error}}

    {{^error}}
            <div class="items-grid">
                {{#items}}
                    <div class="item-card" onclick="location.href='/auction/{{id}}'">
                        <div class="item-name">{{name}}</div>
                        <div class="item-id">ID: {{id}}</div>
                        <div class="item-description">{{description}}</div>
                        <div class="item-price">${{basePrice}}</div>
                        <span class="item-status">{{status}}</span>
                        <a href="/auction/{{id}}" class="btn-view">Ver Subasta</a>
                    </div>
                {{/items}}
            </div>

        {{^items}}
            <div class="empty-state">
                <h2>No hay subastas activas</h2>
                <p>Vuelve más tarde para ver nuevas subastas</p>
            </div>
        {{/items}}
    {{/error}}
</div>
</body>
</html>